services:
  - type: web
    name: perfect-crypto-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python perfect_crypto_bot.py
    plan: free
    envVars:
      - key: PORT
        value: "8080"
